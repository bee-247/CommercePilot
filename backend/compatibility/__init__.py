"""Deprecated compatibility import shims.

New code should import from the canonical packages under core, rag, services,
or orchestrator directly. These modules remain only to keep migrated legacy
imports working during the transition.
"""
